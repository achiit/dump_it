import 'package:flutter/material.dart';

class HotDumpsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Hot Dumps Page'),
    );
  }
}